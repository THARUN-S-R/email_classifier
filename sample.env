# LLM (LiteLLM)
LLM_MODEL=gpt-4o-mini
#OPENAI_API_KEY=.....
# Weaviate
WEAVIATE_URL=http://localhost:8080

# Handler org domains (used to infer internal vs external)
HANDLER_DOMAINS=pinnacle-insurance.co.uk,aviva.com

# Output directory
OUT_DIR=out

# Optional: embedding model (OpenAI example)
EMBED_MODEL=text-embedding-3-small

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# MongoDB (LangChain chat history)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=email_classifier
MONGO_COLLECTION=chat_history
